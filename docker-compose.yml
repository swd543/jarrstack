services:
  gluetun:
    image: qmcgaw/gluetun:latest
    container_name: gluetun
    cap_add:
      - NET_ADMIN
    devices:
      - /dev/net/tun:/dev/net/tun
    expose:
      - "9091"
      - "51413"
    environment:
      - VPN_SERVICE_PROVIDER=protonvpn
      - VPN_TYPE=openvpn
      - OPENVPN_USER=${OPENVPN_USER}
      - OPENVPN_PASSWORD=${OPENVPN_PASSWORD}
      - SERVER_COUNTRIES=${SERVER_COUNTRIES:-Mexico,United States,Singapore,Japan,Canada,Norway,Poland,Romania,Switzerland}
      - TZ=${TZ:-UTC}
      - FIREWALL_INPUT_PORTS=9091,51413
    volumes:
      - ./gluetun:/gluetun
    restart: unless-stopped
    networks:
      - traefik_proxy

  transmission:
    image: lscr.io/linuxserver/transmission:latest
    container_name: transmission
    network_mode: "service:gluetun"
    depends_on:
      - gluetun
    environment:
      - PUID=${PUID:-1000}
      - PGID=${PGID:-1000}
      - TZ=${TZ:-UTC}
    volumes:
      - ./transmission/config:/config
      - ./transmission/downloads:/downloads
      - ./transmission/watch:/watch
    restart: unless-stopped
    labels:
      - traefik.enable=true
      - traefik.docker.network=traefik_proxy
      - traefik.http.routers.transmission.rule=Host(`bugaboxes.local`) && PathPrefix(`/transmission`)
      - traefik.http.routers.transmission.entrypoints=web
      - traefik.http.routers.transmission.priority=100
      - traefik.http.routers.transmission.service=transmission
      - traefik.http.routers.transmission-default.rule=Host(`bugaboxes.local`)
      - traefik.http.routers.transmission-default.entrypoints=web
      - traefik.http.routers.transmission-default.priority=0
      - traefik.http.routers.transmission-default.service=transmission
      - traefik.http.services.transmission.loadbalancer.server.port=9091

networks:
  traefik_proxy:
    external: true
    name: traefik_proxy